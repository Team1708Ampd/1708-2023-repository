package frc.robot;

public class AutoPaths {
    
    /*
     * Variable to hold the Path configuration that will be followed
     * when auto mode runs. 
     */
    int m_PathConfig = 0;


    /*
     * Constructor accepting the path configuration to be used as default
     */
    public AutoPaths(int PathConfiguration)
    {
        m_PathConfig = PathConfiguration;
    }

    /*
     * 
     */
    public void UseNewPath()
    {

    }

}
